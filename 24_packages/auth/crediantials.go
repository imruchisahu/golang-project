package auth

import "fmt"

func LoginWithCredentials(username string, password string){ // if we access this credential use capital letter start ex. LoginWithCredentials 
	fmt.Println("login user using", username, password)
}
